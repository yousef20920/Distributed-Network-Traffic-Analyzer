FROM bitnami/spark:3.5.0

USER root

# Install Python dependencies for PySpark jobs
COPY spark-pyspark/requirements.txt /tmp/requirements.txt
RUN pip install --no-cache-dir -r /tmp/requirements.txt

USER 1001
